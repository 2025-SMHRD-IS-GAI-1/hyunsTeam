package com.supphoto.controller;

import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;
import javax.servlet.http.HttpSession;

import com.google.gson.Gson;
import com.supphoto.frontcontroller.Service;
import com.supphoto.model.UserDAO;
import com.supphoto.model.UserVO;

public class LoginService implements Service {

	@Override
	public String execute(HttpServletRequest request, HttpServletResponse response) {
		String id = request.getParameter("id");
		String pw = request.getParameter("pw");
		UserVO uvo = new UserVO();
		uvo.setUser_id(id);
		uvo.setPassword_hash(pw);
		UserDAO dao = new UserDAO();
		UserVO result = dao.localLogin(uvo);
		Gson gson = new Gson();
		String isValid = "";
		if (result != null) {
			System.out.println("로그인성공");
			HttpSession session = request.getSession();
			isValid = gson.toJson("success");
			session.setAttribute("info", result);
		}else {
			isValid = gson.toJson("fail");
		}
		return "fetch:/" + isValid;
	}

}
